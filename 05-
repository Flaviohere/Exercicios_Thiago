#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    int vetor[n];
    int soma = 0;

    for (int i = 0; i < n; i++) {
        cin >> vetor[i];
        soma += vetor[i];
    }
    
    int media = soma / n;
    printf("Media: %d.0", media);
}
